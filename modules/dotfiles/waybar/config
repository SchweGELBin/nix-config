{
    // Position Top
    "layer": "top",
    "position": "top",
    "margin-bottom": 0,

    // Spacing
    "margin-left": 0,
    "margin-right": 0,
    "spacing": 0,
    "height": 30,

    // Import Modules
    "include": ["~/.config/waybar/modules.json"],

    // Modules left
    "modules-left": [
        "hyprland/workspaces"
    ],

    // Modules Center
    "modules-center": [
        "hyprland/window"
    ],

    // Modules Right
    "modules-right": [
	"cpu",
	"memory",
	"temperature",
	"pulseaudio",
	"clock"
    ]
}
